# 空格字符缺失修复

## 问题描述

在使用"添加所有ASCII可见字符"功能时，发现生成的字体缺少空格字符。

## 根本原因

在 `src/characterinputwidget.cpp` 的 `addAllASCII()` 函数中，循环从ASCII码33开始，而不是32：

```cpp
// 错误的实现
for (int i = 33; i <= 126; i++) {  // 从'!'开始，跳过了空格
    ascii.append(QChar(i));
}
```

ASCII码对照：
- **32 = 空格 (space)** ← 被跳过了
- 33 = '!'
- 34 = '"'
- ...
- 126 = '~'

## 修复方案

将起始值从33改为32，包含空格字符：

```cpp
// 正确的实现
for (int i = 32; i <= 126; i++) {  // 从空格开始
    ascii.append(QChar(i));
}
```

## ASCII可见字符范围

根据ASCII标准：
- **可打印字符**：32-126（95个字符）
  - 32：空格（虽然不可见，但是可打印）
  - 33-126：可见字符（94个）

- **不可打印字符**：0-31, 127
  - 控制字符（如换行、制表符等）

## 为什么空格很重要？

1. **文本渲染**：单词之间需要空格分隔
2. **布局计算**：空格有前进宽度（advance width）
3. **完整性**：ASCII可见字符集应该包含空格

## 影响

修复前：
- "添加所有ASCII可见字符"生成94个字符（缺少空格）
- 显示文本时单词会连在一起

修复后：
- "添加所有ASCII可见字符"生成95个字符（包含空格）
- 文本显示正常，单词正确分隔

## 验证

1. 重新编译工具
2. 使用"添加所有ASCII可见字符"功能
3. 检查字符输入框，应该包含空格
4. 生成字体并测试显示效果

## 相关文件

- `src/characterinputwidget.cpp` - 修复ASCII字符范围

## 技术细节

### ASCII字符分类

```
0-31:   控制字符（不可打印）
32:     空格（可打印但不可见）
33-126: 可见字符（可打印且可见）
127:    DEL（不可打印）
```

### 常见的ASCII范围定义

- **可打印字符**：32-126（包含空格）
- **可见字符**：33-126（不包含空格）
- **字母数字**：48-57, 65-90, 97-122

在字体生成的上下文中，"ASCII可见字符"通常指**可打印字符**（包含空格），因为空格虽然不可见，但在文本渲染中是必需的。

## 建议

考虑重命名菜单项以更准确：
- 当前：`"所有ASCII可见字符"`
- 建议：`"所有ASCII可打印字符 (32-126)"` 或 `"ASCII可见字符+空格"`

这样可以明确告诉用户包含了空格字符。
