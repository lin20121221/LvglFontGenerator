# BPP格式支持文档

## 概述

LVGL字体生成工具现在支持所有标准的BPP（Bits Per Pixel）格式：
- **1bpp**: 单色（黑白）
- **2bpp**: 4级灰度
- **4bpp**: 16级灰度
- **8bpp**: 256级灰度

## 格式详细说明

### 1bpp - 单色格式

**特点：**
- 每像素1位
- 每字节存储8个像素
- 值范围：0（背景）或 1（前景）

**转换规则：**
```cpp
pixel = (gray > 127) ? 1 : 0;  // 阈值为127
```

**打包方式：**
- MSB first（最高位是第一个像素）
- 8个像素打包成1个字节
- 例如：像素 [0,0,0,1,1,1,1,1] → 0x1F

**位图大小计算：**
```cpp
bytes = (width * height + 7) / 8
```

**适用场景：**
- 简单图标
- 单色文字
- 最小存储空间

---

### 2bpp - 4级灰度

**特点：**
- 每像素2位
- 每字节存储4个像素
- 值范围：0-3（4个灰度级别）

**转换规则：**
```cpp
pixel = gray >> 6;  // 右移6位，等同于 gray / 64
```

**打包方式：**
- MSB first
- 4个像素打包成1个字节
- 例如：像素 [0,1,2,3] → 0x1B (00 01 10 11)

**位图大小计算：**
```cpp
bytes = (width * height + 3) / 4
```

**适用场景：**
- 简单抗锯齿
- 平衡质量和大小
- 低端设备

---

### 4bpp - 16级灰度

**特点：**
- 每像素4位
- 每字节存储2个像素
- 值范围：0-15（16个灰度级别）

**转换规则：**
```cpp
pixel = gray >> 4;  // 右移4位，等同于 gray / 16
```

**打包方式：**
- 高4位存储第一个像素，低4位存储第二个像素
- 2个像素打包成1个字节
- 例如：像素 [2,15] → 0x2F

**位图大小计算：**
```cpp
bytes = (width * height + 1) / 2
```

**适用场景：**
- 标准抗锯齿文字
- 官方lv_font_conv默认格式
- 质量和大小的最佳平衡

---

### 8bpp - 256级灰度

**特点：**
- 每像素8位
- 每字节存储1个像素
- 值范围：0-255（256个灰度级别）

**转换规则：**
```cpp
pixel = gray;  // 直接使用，无需转换
```

**打包方式：**
- 无需打包，直接存储
- 1个像素占1个字节

**位图大小计算：**
```cpp
bytes = width * height
```

**适用场景：**
- 高质量渲染
- 平滑抗锯齿
- 存储空间充足的设备

---

## 位图大小对比

以10x10像素的字形为例（100像素）：

| BPP | 字节数 | 压缩率 | 质量 |
|-----|--------|--------|------|
| 1bpp | 13 | 87% | 低 |
| 2bpp | 25 | 75% | 中低 |
| 4bpp | 50 | 50% | 中高 |
| 8bpp | 100 | 0% | 高 |

## 实现细节

### 像素打包顺序

所有格式都使用**MSB first**（最高位优先）的打包方式：

**1bpp示例：**
```
像素: [p0, p1, p2, p3, p4, p5, p6, p7]
字节: [p0<<7 | p1<<6 | p2<<5 | p3<<4 | p4<<3 | p5<<2 | p6<<1 | p7]
```

**2bpp示例：**
```
像素: [p0, p1, p2, p3]
字节: [p0<<6 | p1<<4 | p2<<2 | p3]
```

**4bpp示例：**
```
像素: [p0, p1]
字节: [p0<<4 | p1]
```

### 边界处理

当像素数量不是打包单位的整数倍时，剩余位填充0：

**1bpp示例（5个像素）：**
```
像素: [1, 0, 1, 1, 0]
字节: 0xB0 (10110000)
      ^^^^^ 实际数据
          ^^^ 填充0
```

**4bpp示例（奇数宽度）：**
```
像素: [15, 8, 3]
字节: [0xF8, 0x30]
       ^^^^ 前两个像素
            ^^^^ 第三个像素+填充
```

## 代码实现

### 位图生成（lvglexporter.cpp）

```cpp
if (m_bpp == 1) {
    // 1bpp: 8 pixels per byte
    for (int x = 0; x < width; x += 8) {
        int packed = 0;
        for (int bit = 0; bit < 8 && (x + bit) < width; bit++) {
            int gray = qGray(img.pixel(x + bit, y));
            int pixel = (gray > 127) ? 1 : 0;
            packed |= (pixel << (7 - bit));
        }
        output(packed);
    }
} else if (m_bpp == 2) {
    // 2bpp: 4 pixels per byte
    // ...
} else if (m_bpp == 4) {
    // 4bpp: 2 pixels per byte
    // ...
} else {
    // 8bpp: 1 pixel per byte
    // ...
}
```

### 偏移量计算

```cpp
int totalPixels = width * height;
if (m_bpp == 1) {
    offset += (totalPixels + 7) / 8;
} else if (m_bpp == 2) {
    offset += (totalPixels + 3) / 4;
} else if (m_bpp == 4) {
    offset += (totalPixels + 1) / 2;
} else {
    offset += totalPixels;
}
```

## 选择建议

### 推荐使用4bpp
- 官方lv_font_conv默认格式
- 质量和大小的最佳平衡
- 适合大多数应用场景

### 使用1bpp的情况
- 存储空间极度受限
- 只需要简单图标
- 不需要抗锯齿

### 使用2bpp的情况
- 需要基本抗锯齿
- 存储空间有限
- 低端MCU

### 使用8bpp的情况
- 需要最高质量
- 存储空间充足
- 高端设备

## 测试

运行测试脚本验证所有格式：
```bash
python test_all_bpp_formats.py
```

## 兼容性

所有格式与LVGL 8.x和9.x完全兼容。
