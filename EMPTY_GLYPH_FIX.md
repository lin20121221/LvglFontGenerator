# 空格字符位图数据修复

## 问题描述

当生成包含空格字符的字体时，空格字符会生成1字节的位图数据（`0x00`），而官方工具对空字符不生成任何位图数据。

```c
// 当前工具（错误）
/* U+0020 " " */
0x00,

// 官方工具（正确）
// 没有U+0020的位图数据
```

## 根本原因

在 `src/fontgenerator.cpp` 中，当字形的宽度或高度为0时（空格就是这种情况），代码创建了一个1×1的黑色图像：

```cpp
// 错误的实现
} else {
    glyph.bitmap = QImage(1, 1, QImage::Format_Grayscale8);
    glyph.bitmap.fill(0);
}
```

这导致：
- 空格字符有1×1像素的位图
- 4bpp格式下生成1字节数据（0x00）
- 与官方工具不一致

## 空格字符的特点

空格字符（U+0020）的特殊性：
- **宽度**：0像素（FreeType返回width=0）
- **高度**：0像素（FreeType返回height=0）
- **前进宽度**：有值（例如4-5像素）
- **位图**：无（不需要绘制任何内容）

空格只需要前进宽度信息，不需要位图数据。

## 修复方案

创建一个**空图像**（QImage()），而不是1×1的图像：

```cpp
// 正确的实现
} else {
    // 对于空字形（如空格），创建空图像，不生成位图数据
    glyph.bitmap = QImage();
}
```

## 修复效果

### 修复前
```c
/* U+0020 " " */
0x00,

/* U+0021 "!" */
0x1f, 0xf6, ...
```

字形描述符：
```c
{.bitmap_index = 0, .adv_w = 64, .box_w = 1, .box_h = 1, ...}  // 空格
{.bitmap_index = 1, .adv_w = 69, .box_w = 4, .box_h = 11, ...} // !
```

### 修复后
```c
/* U+0021 "!" */
0x1f, 0xf6, ...
```

字形描述符：
```c
{.bitmap_index = 0, .adv_w = 64, .box_w = 0, .box_h = 0, ...}  // 空格
{.bitmap_index = 0, .adv_w = 69, .box_w = 4, .box_h = 11, ...} // !
```

注意：
- 空格的 `box_w` 和 `box_h` 都是0
- 空格和'!'的 `bitmap_index` 都是0（因为空格没有位图数据）
- 空格仍然有 `adv_w`（前进宽度）

## QImage的行为

```cpp
QImage()                    // 空图像，isNull()=true, width()=0, height()=0
QImage(1, 1, Format)        // 1×1图像，isNull()=false, width()=1, height()=1
```

在导出时，空图像不会生成任何位图数据。

## 影响的字符

这个修复影响所有没有可见字形的字符：
- **空格**（U+0020）- 最常见
- **制表符**（U+0009）- 如果包含的话
- **其他空白字符** - 如果包含的话

## 验证

1. 重新编译工具
2. 生成包含空格的字体
3. 检查生成的.c文件：
   - 空格字符不应该有位图数据
   - 空格的box_w和box_h应该是0
   - 空格应该有正确的adv_w

## 相关文件

- `src/fontgenerator.cpp` - 修复空字形的位图创建

## 技术细节

### LVGL如何处理空字形

LVGL在渲染时：
1. 检查字形的 `box_w` 和 `box_h`
2. 如果都是0，不绘制任何内容
3. 只使用 `adv_w` 来移动光标位置

这就是为什么空格不需要位图数据的原因。

### 为什么之前创建1×1图像？

可能是为了避免空指针或空图像的问题，但实际上：
- QImage() 是安全的
- LVGL可以正确处理box_w=0和box_h=0的情况
- 创建1×1图像反而会浪费空间

## 总结

这个修复确保空格等空白字符：
- ✅ 不生成无用的位图数据
- ✅ 与官方工具行为一致
- ✅ 节省字体文件大小
- ✅ 正确显示（只移动光标，不绘制）
