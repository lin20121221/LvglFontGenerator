# 字符检查功能测试说明

## 修改内容

已将CharacterInputWidget修改为显示TTF字库中实际包含的字符：

1. **字符存在于字体中**：显示实际字符
2. **字符不存在于字体中**：显示方框（□，Unicode U+25A1）
3. **不可见字符**：显示方框（□）

## 测试步骤

1. 启动程序
2. 选择一个TTF字体文件
3. 在字符输入框中右键添加字符（如"添加数字"、"添加中文"等）
4. 观察显示结果：
   - 字体中存在的字符会正常显示
   - 字体中不存在的字符会显示为方框□

## 实现细节

- 使用FreeType的`FT_Get_Char_Index()`检查字符是否在字体中
- 如果返回的glyph索引为0，表示字符不存在
- 方框符号使用Unicode U+25A1（WHITE SQUARE）

## 性能说明

每次调用`updateDisplay()`时会检查所有字符，对于大量字符可能会有性能开销。如果需要优化，可以考虑：
1. 缓存字体face对象而不是每次重新加载
2. 缓存字符检查结果
