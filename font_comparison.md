# 字库对比分析

## 字符 '0' 的详细对比

### 官方工具 (My_Font_1.c)
```
.bitmap_index = 0
.adv_w = 144        // 9 像素 × 16 = 144
.box_w = 9          // 宽度 9 像素
.box_h = 12         // 高度 12 像素
.ofs_x = 0          // X偏移 0
.ofs_y = 0          // Y偏移 0
line_height = 12    // 行高 12
base_line = 0       // 基线 0
```

### 本工具 (My_Font.c) - 当前版本
```
.bitmap_index = 0
.adv_w = 192        // 12 像素 × 16 = 192
.box_w = 10         // 宽度 10 像素
.box_h = 15         // 高度 15 像素
.ofs_x = 1          // X偏移 1
.ofs_y = 0          // Y偏移 0
line_height = 15    // 行高 15 (修改后)
base_line = 0       // 基线 0
```

## 差异分析

### 1. 字符前进宽度 (adv_w)
- **差异**: 192 vs 144 (相差 48，即 3 像素)
- **影响**: 字符间距会更宽
- **原因**: Qt 的 horizontalAdvance() 返回 12，而 FreeType 返回 9

### 2. 字形尺寸 (box_w × box_h)
- **差异**: 10×15 vs 9×12
- **影响**: 字形更大，可能超出预期显示区域
- **原因**: Qt 和 FreeType 的字形边界框计算不同

### 3. X偏移 (ofs_x)
- **差异**: 1 vs 0
- **影响**: 字形向右偏移 1 像素
- **原因**: Qt 的 boundingRect.x() 返回 1

### 4. 行高 (line_height)
- **差异**: 15 vs 12 (相差 3 像素)
- **影响**: 行间距更大
- **原因**: 字形高度不同

## 可能的显示问题

### 如果字符显示不完整（被裁剪）
- **原因**: box_h = 15 太高，超出了显示区域
- **解决**: 需要裁剪字形的上下空白

### 如果字符位置偏移
- **原因**: ofs_x, ofs_y 或 base_line 计算不正确
- **解决**: 需要调整坐标系统

### 如果字符间距太宽
- **原因**: adv_w = 192 太大
- **解决**: 需要调整 horizontalAdvance 的计算

## 根本原因

**Qt 的字体渲染与 FreeType 不同：**
1. 边界框计算方式不同
2. 字形度量值不同
3. 渲染的位图数据不同

**要完全匹配官方工具，需要：**
1. 使用 FreeType 库替代 Qt 的字体渲染
2. 或者对 Qt 的结果进行后处理（裁剪、调整）
