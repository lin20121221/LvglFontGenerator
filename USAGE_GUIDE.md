# 新增功能说明

## 1. 字体选择方式

### 方式一：浏览文件
点击"浏览..."按钮，从文件系统中选择字体文件（.ttf, .otf, .ttc）。
- 对话框会自动在侧边栏添加Windows字体目录快捷方式
- 系统字体目录：`C:\Windows\Fonts`
- 用户字体目录：`%USERPROFILE%\AppData\Local\Microsoft\Windows\Fonts`

### 方式二：系统字体（推荐）
点击"系统字体..."按钮，从已安装的系统字体列表中选择：
- 显示所有已安装的系统字体
- 支持搜索功能，快速查找字体
- 双击字体名称或点击"确定"按钮完成选择
- 自动定位字体文件路径

**使用技巧：**
- 如果知道字体名称（如"微软雅黑"、"Arial"），使用"系统字体"更方便
- 如果有自定义字体文件，使用"浏览"功能

## 2. 字符输入右键菜单

在字符设置输入框中点击右键，可以快速添加或删除字符集：

### 添加字符
- **数字 (0-9)**: 添加所有数字
- **大写字母 (A-Z)**: 添加所有大写英文字母
- **小写字母 (a-z)**: 添加所有小写英文字母
- **常用中文 (3500字)**: 添加国家语委现代汉语常用字表中的常用字
- **基本标点符号**: 添加常用标点和特殊符号
- **所有ASCII可见字符**: 添加ASCII 33-126的所有可见字符

### 删除字符
- **删除所有数字**: 从输入中移除所有数字
- **删除所有大写字母**: 移除所有大写英文字母
- **删除所有小写字母**: 移除所有小写英文字母
- **删除所有中文**: 移除所有中文字符（CJK统一汉字）

### 其他功能
- **清除重复字符**: 自动去重，保留每个字符的第一次出现
- **清空全部**: 清空所有输入

### 使用技巧
1. 可以多次添加不同字符集，系统会自动避免重复
2. 先添加大字符集，再删除不需要的部分
3. 最后使用"清除重复字符"确保没有重复

## 2. 字体预览右键菜单

在字体预览区域点击右键，可以快速调整视图：

### 菜单选项
- **居中显示**: 将预览内容自动居中到窗口中央
- **重置位置**: 将预览位置重置到左上角(0, 0)

### 操作方式
- **鼠标左键拖拽**: 平移预览内容
- **鼠标滚轮**: 缩放预览
- **右键菜单**: 快速居中或重置

## 3. 抗锯齿选项说明

### 1位 (无抗锯齿 - 纯黑白)
- 每个像素只有黑或白两种状态
- 适合单色屏幕（如OLED、墨水屏）
- 文件最小，但边缘锯齿明显

### 2位 (4级灰度)
- 每个像素有4种灰度级别（0%, 33%, 67%, 100%）
- 轻度抗锯齿，适合低灰度屏幕
- 文件较小，边缘稍微平滑

### 4位 (16级灰度)
- 每个像素有16种灰度级别
- 中度抗锯齿，适合灰度屏幕
- 文件适中，边缘较平滑

### 8位 (256级灰度)
- 每个像素有256种灰度级别
- 完全抗锯齿，适合彩色屏幕
- 文件最大，边缘最平滑

### 选择建议
| 屏幕类型 | 推荐BPP | 说明 |
|---------|---------|------|
| 单色OLED | 1位 | 无抗锯齿，纯黑白 |
| 灰度屏 | 2位或4位 | 根据屏幕支持的灰度级别选择 |
| 彩色TFT | 8位 | 最佳显示效果 |
| 小字体(<12px) | 1位或2位 | 抗锯齿效果不明显 |
| 大字体(≥16px) | 4位或8位 | 抗锯齿效果显著 |

## 使用示例

### 示例1: 生成数字显示字体
1. 右键字符输入框 → 添加字符 → 数字(0-9)
2. 右键字符输入框 → 添加字符 → 基本标点符号
3. 选择合适的字体大小和抗锯齿级别
4. 生成字体

### 示例2: 生成中英文混合字体
1. 右键字符输入框 → 添加字符 → 大写字母(A-Z)
2. 右键字符输入框 → 添加字符 → 小写字母(a-z)
3. 右键字符输入框 → 添加字符 → 常用中文(3500字)
4. 右键字符输入框 → 清除重复字符
5. 生成字体

### 示例3: 自定义字符集
1. 右键字符输入框 → 添加字符 → 所有ASCII可见字符
2. 右键字符输入框 → 删除字符 → 删除所有小写字母
3. 手动添加特殊字符（如"℃", "±", "×"等）
4. 右键字符输入框 → 清除重复字符
5. 生成字体

## 快捷键

| 操作 | 快捷键/方式 |
|------|------------|
| 添加/删除字符 | 右键字符输入框 |
| 居中预览 | 右键预览区域 → 居中显示 |
| 缩放预览 | 鼠标滚轮 或 +/- 按钮 |
| 平移预览 | 鼠标左键拖拽 |
| 重置预览 | 右键预览区域 → 重置位置 |
