# 灰度映射修复

## 问题发现

在检查灰度等级映射时，发现当前工具使用的是**比例映射**，而官方工具使用的是**位移映射**。

## 两种映射方法对比

### 方法1：比例映射（修复前）
```cpp
// 2bpp: pixel = (gray * 3 + 127) / 255
// 4bpp: pixel = (gray * 15 + 127) / 255
```

### 方法2：位移映射（官方工具）
```cpp
// 2bpp: pixel = gray >> 6  // 右移6位
// 4bpp: pixel = gray >> 4  // 右移4位
// 通用: pixel = gray >> (8 - bpp)
```

## 差异分析

| 8位输入 | 位移>>4 | 比例映射 | 差异 |
|---------|---------|----------|------|
| 0       | 0       | 0        | 0    |
| 32      | 2       | 2        | 0    |
| 64      | 4       | 4        | 0    |
| 96      | 6       | 6        | 0    |
| 128     | 8       | 8        | 0    |
| 160     | 10      | 9        | **1** |
| 192     | 12      | 11       | **1** |
| 224     | 14      | 13       | **1** |
| 255     | 15      | 15       | 0    |

**在高灰度值（160+）时，两种方法有1个灰度级的差异。**

## 官方工具的实现

在 `lib/font/table_glyf.js` 第27行：

```javascript
// Convert 8-bit opacity to bpp-bit.
reduceBpp(pixels) {
  const bpp = this.font.opts.bpp;
  return pixels.map(line => line.map(p => (p >>> (8 - bpp))));
}
```

- `>>>` 是无符号右移运算符
- 对于4bpp：`p >>> (8 - 4)` = `p >>> 4`
- 对于2bpp：`p >>> (8 - 2)` = `p >>> 6`

## 修复方案

修改 `src/lvglexporter.cpp`，使用位移映射：

```cpp
// 2bpp
int pixel = gray >> 6;  // 右移6位，等同于 gray / 64

// 4bpp  
int pixel = gray >> 4;  // 右移4位，等同于 gray / 16
```

## 为什么官方工具使用位移？

1. **性能**：位移比乘除法快得多
2. **简单**：代码更简洁，不需要舍入
3. **一致性**：对所有bpp使用统一的公式

## 修复效果

### 修复前（比例映射）
```
灰度192 → (192 * 15 + 127) / 255 = 11
灰度224 → (224 * 15 + 127) / 255 = 13
```

### 修复后（位移映射）
```
灰度192 → 192 >> 4 = 12 ✓
灰度224 → 224 >> 4 = 14 ✓
```

## 影响评估

这个修复将使生成的位图数据与官方工具**更加接近**，特别是在高灰度值区域。

之前观察到的22%像素差异，很大一部分可能就是由这个映射方法的差异造成的。

## 验证

修复后重新生成字体并运行：

```bash
python compare_bitmaps_detailed.py
```

预期结果：
- 位图差异应该显著减少
- 高灰度值的像素应该更加匹配

## 相关文件

- `src/lvglexporter.cpp` - 修复2bpp和4bpp的灰度映射
- `test_grayscale_mapping.py` - 灰度映射测试工具
